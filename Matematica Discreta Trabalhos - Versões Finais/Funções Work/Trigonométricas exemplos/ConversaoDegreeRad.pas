program ConversaoDegreeRad;

{ Program to demonstrate the degtorad function. }

Uses crt, math;
//function degtorad(
  //deg: float
//):float;
// converte graus para radianos - detalhe: float

begin
  writeln(degtorad(45));
  writeln(degtorad(90));
  writeln(degtorad(180));
  writeln(degtorad(270));
  writeln(degtorad(360));
  readkey;
end.

